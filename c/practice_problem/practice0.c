#include <stdio.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    long long int x,y,multi;

    printf("");
    scanf("%lld %lld",&x,&y);
    multi = x * y;
    printf("%lld",multi);
    return 0;
}