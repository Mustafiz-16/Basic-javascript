#include<stdio.h>
int main(){
    float x = 5;
    float y = 2;
    float z = 5 /2;/*when we work on a two integer number then the answer will be come form a integer .
    But when one float is available then the answer was floating*/ 
    printf("%f",z);
    return 0;
}